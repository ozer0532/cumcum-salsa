

// Mengubah state pemesanan dari nama ke jumlah, jumlah ke alamat, dst.
async function setStatePesan (context, newState) {
    
}

// Mengoutput status pemesanan dari user tersebut
async function getStatePesan (context) {
    let state;

    return state;
}

// Menyimpan kode pemesanan pengguna ke database
async function setKodePesan (context) {
    
}

// Mengoutput id pengguna yang memiliki pemesanan yang sesuai dengan kode
async function getUserPesan (kode) {
    let userId;

    return userId;
}